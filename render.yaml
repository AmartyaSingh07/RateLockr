services:
  - type: web
    name: ratelockr-api
    runtime: node
    plan: free
    rootDir: service
    buildCommand: "npm install && npm run build"
    startCommand: "node dist/app.js"
    envVars:
      - key: NODE_ENV
        value: production
