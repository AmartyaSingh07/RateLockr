services:
  - type: web
    name: ratelockr-api
    runtime: node
    plan: free
    rootDirectory: api
    buildCommand: "npm install --include=dev && npm run build"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
